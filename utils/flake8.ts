// import { loadPyodide } from '@/pyodide/evaluator';

export async function runFlake8(code: string): Promise<string[]> {
  // Flake8 functionality is disabled because Pyodide loading is not available here.
  return [];
}
